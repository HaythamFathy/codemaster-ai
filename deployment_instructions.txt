DEPLOYMENT INSTRUCTIONS FOR VERCEL
==================================

**CRITICAL FIX:** In your previous attempt, you typed "(default)" into the settings field. You must NOT type that.

1. Push to GitHub
-----------------
(You have already promoted the code, so you can skip this if you've pushed the latest package.json).

2. Import/Configure in Vercel
-----------------------------
- Go to your Project Settings in Vercel (or create new).
- **Framework Preset**: Next.js
- **Root Directory**: `./` 

**BUILD & DEVELOPMENT SETTINGS (IMPORTANT):**
You must type these EXACTLY as shown below. Do not add any extra text.

- **Build Command**: `npm run build`
- **Output Directory**: `frontend/.next`
- **Install Command**: `npm install`
  (Leave it as `npm install` or OVERRIDE it with that text. DO NOT type "(default)")

3. Environment Variables
------------------------
(Same as before)
   SECRET_KEY                   <random_string>
   ALGORITHM                    HS256
   ACCESS_TOKEN_EXPIRE_MINUTES  30
   ALLOWED_ORIGINS              https://your-project.vercel.app,http://localhost:3000

4. Redeploy
-----------
- Go to the "Deployments" tab in Vercel.
- Click the three dots on the failed deployment -> "Redeploy".
- Make sure the settings match above.
